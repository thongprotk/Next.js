# Cross-cutting rules

Chỉ thêm file `.md` vào đây khi **một fact áp dụng cho ≥2 skills** và chưa nằm trong bất kỳ `SKILL.md` nào.

Hiện tại Demo chỉ có skill `cms` → thư mục này cố ý trống (ngoài README này).
